# Godot 4+ specific ignores
.godot/

# Godot-specific ignores
.import/
export.cfg
export_presets.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json
/test
.opencode

# GDExtension 构建产物（编译出的动态库不入库，由 GitHub Actions 构建后发布）
native/build/
native/bin/*.dylib
native/bin/*.so
native/bin/*.dll
*.gdextension.uid
